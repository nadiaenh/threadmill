[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "threadmill"
version = "0.1.0"
readme = "README.md"
description = "Trusted same-process Python background jobs with bounded worker lanes"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
